# Lean dependency set for batch jobs (GitHub Actions) — no Streamlit.
# The app's full set is requirements.txt.
supabase>=2.5
python-dotenv>=1.0
pydantic>=2.7
httpx>=0.27
beautifulsoup4>=4.12
pyyaml>=6.0
groq>=0.13
google-genai>=1.0

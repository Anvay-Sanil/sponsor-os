# Sponsor OS — Streamlit Community Cloud requirements.
# RULE: heavy ML deps (torch, transformers, numpyro, xgboost, sentence-transformers)
# must NEVER be added here — they run in Colab notebooks only. The free Streamlit
# tier cannot hold them in memory. Embeddings use the Gemini API (768-dim), not a
# local model.
streamlit>=1.37
supabase>=2.5
python-dotenv>=1.0
pydantic>=2.7
pytest>=8.2
# Phase 2 (Scout): scraping + LLM SDKs — all lightweight, no torch anywhere.
httpx>=0.27
beautifulsoup4>=4.12
pyyaml>=6.0
groq>=0.13
# google-genai replaced the EOL'd google-generativeai (Phase 6 migration);
# it also powers search-grounded brand research and 768-dim embeddings.
google-genai>=1.0
# Phase 3 (Pitch): deck rendering + palette extraction — light wheels, no torch.
python-pptx>=1.0
Pillow>=10.0
# Phase 6: session persistence — stores ONLY the Supabase refresh token in a
# 7-day cookie so juniors aren't re-typing passwords every visit.
extra-streamlit-components>=0.1.80
